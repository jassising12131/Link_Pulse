{name: Keep Render awake, on: {schedule: [{cron: '*/5 * * * *'}], workflow_dispatch: {}}, jobs: {ping: {runs-on: ubuntu-latest, steps: [{run: 'curl -fsS --max-time 60 https://linkpulse-z9xa.onrender.com/api/health || true'}]}}}
